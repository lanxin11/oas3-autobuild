type: oauth2
description: OAuth bearer token for authentication.
flows:
  clientCredentials:
    tokenUrl: https://rest.apisandbox.zuora.com/oauth/token
    scopes: {
      "write": "create, update, or delete an object",
      "read:" "read the info on an object"
    }
