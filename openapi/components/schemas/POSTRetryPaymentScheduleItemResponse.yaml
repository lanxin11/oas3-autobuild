properties:
  items:
    items:
      $ref: ./PaymentScheduleItemCommonResponse.yaml
    type: array
  success:
    description: |
      Returns `true` if the request was processed successfully. 
    type: boolean
type: object
