allOf:
  - properties:
      amount:
        description: >
          The amount of the invoice to be generated during the processing of the
          invoice schedule item.
        format: number
        type: string
      id:
        description: >
          The unique ID of the invoice schedule item to be updated. 


          If this field is not provided, a new invoice schedule item is added to
          the invoice schedule.
        type: string
      runDate:
        description: >
          The date in the tenant’s time zone when the invoice schedule item is
          planned to be processed to generate an invoice.
        format: date
        type: string
    type: object
  - {}
title: scheduleItems
