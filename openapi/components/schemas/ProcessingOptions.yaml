description: Processing options for generating billing documents.
properties:
  billingOptions:
    $ref: ./BillingOptions.yaml
  runBilling:
    description: >-
      Indicates if the current request needs to generate a billing document. The
      billing document will be generated against all Order Line Items included
      in this order.
    type: boolean
type: object
