allOf:
  - properties:
      accountKey:
        description: |
          The ID or number of the account associated with the invoice schedule.
        type: string
      notes:
        description: |
          Comments on the invoice schedule.
        maxLength: 255
        type: string
      orders:
        description: >
          A list of the IDs or numbers of the orders associated with the invoice
          schedule. One invoice schedule can be associated with at most 10
          orders.
        items:
          type: string
        type: array
      scheduleItems:
        description: >
          Container for invoice schedule items. One invoice schedule can have at
          most 50 invoice schedule items.
        items:
          $ref: ./POSTScheduleItemType.yaml
        type: array
      specificSubscriptions:
        description: >
          A list of the numbers of specific subscriptions associated with the
          invoice schedule.


          - If the subsriptions specified in this field belong to the orders
          specified in the `orders` field, only the specific subscriptions
          instead of the orders are associated with the invoice schedule. 

          - If only the `orders` field is specified, all the subscriptions from
          the order are associated with the invoice schedule.
        items:
          $ref: ./InvoiceScheduleSpecificSubscriptions.yaml
        type: array
    type: object
  - {}
example:
  accountKey: A00000007
  notes: 2022 Billing Schedule
  orders:
    - O-00000007
    - O-00000008
  scheduleItems:
    - amount: 50000
      runDate: '2022-02-24'
    - amount: 14000
      runDate: '2022-10-17'
    - amount: 6200
      runDate: '2022-11-14'
  specificSubscriptions:
    - orderKey: O-00000008
      subscriptionKey: S-00000008
