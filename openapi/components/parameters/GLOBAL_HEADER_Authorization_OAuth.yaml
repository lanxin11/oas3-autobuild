name: Authorization
in: header
required: false
description: |
  `Bearer {token}` for a valid OAuth token.
schema:
  type: string
  format: 'bearer {token}'
examples:
  Authorization:
    summary: Authorization example
    value: "Bearer 6d151216ef504f65b8ff6e9e9e8356d3"

